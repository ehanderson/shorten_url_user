# helpers do
#   # This will return the current user, if they exist
#   # Replace with code that works with your application
#   def current_user
#     if session[:authetic]
#       @current_user ||= User.find_by_id(session[:authetic])
#     end
#   end

#   # Returns true if current_user exists, false otherwise
#   def logged_in?
#     !current_user.nil?
#   end
# end
# #############THIS IS WHERE SHIT WONT CONTINUE###########
# @user = User.find_by_id(current_user.id)
# @url = Url.create(...)
# @user.urls << @url

